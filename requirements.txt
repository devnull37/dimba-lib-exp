# Core runtime dependencies
torch>=2.0.0
pytorch-lightning>=2.0.0
datasets>=2.13.0
numpy>=1.24.0
PyYAML>=6.0.0
tokenizers>=0.13.0
tqdm>=4.65.0

# First-party scripts rely on these in normal workflows
transformers>=4.30.0
tensorboard>=2.12.0
huggingface_hub>=0.20.0

# Test dependency
pytest>=7.0.0

# Optional: optimized GPU Mamba kernels
# mamba-ssm>=2.2.0
# causal-conv1d>=1.4.0

# Optional: evaluation metrics
# sacrebleu>=2.3.1
# rouge-score>=0.1.2
# nltk>=3.8.0

# Optional: experiment tracking and quantized finetuning
# wandb>=0.14.0
# bitsandbytes>=0.43.0
